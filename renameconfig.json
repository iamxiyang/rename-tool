// 该文件是JSONC格式，允许写注释
{
  "input": {
    "glob": "**",
    "fast-glob": {}
  },
  "output": {
    // 保存到什么目录，TODO 暂不支持
    "path": "./",
    // 文件默认规则
    "filename": "{pinyin}",
    "mapping": {
      // 映射关系优先级最高，key是原始文件名，不包含后缀
      // "": ""
    }
  }
}